import React from "react";
import filter from "./filter.module.scss";

const Filter = () => {
  return (
    <div className={filter.mainContainer}>
      <div className={filter.holder}>fdjkfd</div>
      <div className={filter.holder}>fdjkfd</div>
    </div>
  );
};

export default Filter;
